node_modules/
dist/
.mastra/
web/dist/
*.tsbuildinfo

# Local data: workflow snapshots, memory, scorer results, vector index
data/
*.db
*.sqlite

# Private knowledge corpus (only synthetic samples are committed). Ignore the
# corpus contents but re-include the samples directory — this must exclude children
# (`knowledge/*`), not the directory itself, or the negation can't take effect.
knowledge/*
!knowledge/samples/

# Secrets
.env
.env.*
!.env.example

# Local CV inputs (personal documents, never shipped)
cv/

# Planning notes and task files (source of truth lives in the working checkout only)
plans/

# Task review output (branch-scoped, ephemeral)
reviews/
